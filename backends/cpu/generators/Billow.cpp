#include "Billow.hpp"

void cpuBillowLauncher(float* out, const int& width, const int& height, const float& origin_x, const float& origin_y, 
                             const float& freq, const float& lacunarity, const float& persistence, const int& octaves, const int& seed) {
    for(int i = 0; i < width; ++i) {
        for(int j = 0; j < height; ++j) {
            
        }
    }
}